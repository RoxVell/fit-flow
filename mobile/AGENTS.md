# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Prefer the Expo MCP server (plugin `expo@claude-plugins-official`, enabled in the
repo's `.claude/settings.json`) for docs, `expo_router_sitemap`, screenshots and
logs. See `README.md` in this folder for the app layout and current status.
